{
    "rewrites": [
      { "source": "/(.*)", "destination": "/" },
            {
      "source": "/api/:path*",  // Match all routes starting with /api/
      "destination": "https://your-backend-project.vercel.app/api/:path*" // Forward to backend
    },
    ]
  }
